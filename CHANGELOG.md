# Changelog

## 当前

- **外观主题作用于所有弹窗**：鲸鱼娘 / 纯白 / 纯黑三种风格切换同时应用于配置弹窗、自定义上传弹窗与 AI 生成弹窗（此前仅配置弹窗跟随），全程统一观感。
- **教程配图优化**：
  - 详细教程配图宽度占满容器、高度等比放大（不再限制高度）；
  - 点击图片放大查看：放大为展示框内图的 3 倍，等比缩放到不超过视口 70%（且不小于原图），放大图可按住拖动查看超出的部分；点击遮罩关闭；
  - 修复放大层被弹窗覆盖的问题（放大层 z-index 与弹窗同级）。
- **弹窗层级修复（maid-atelier 皮肤）**：皮肤上边框壁纸（top-trim）与工作区头部按钮（workspace header）此前会盖住弹窗，插件注入样式将其层级降至弹窗之下——弹窗（含放大层）始终显示在最上层。

## 1.2.0 (2026-08-18)

- **包名更名**：npm 包名由 `dsh-client-ui-event-sounds` 改为 `dsh-sound-lab`（与仓库名一致），
  安装命令、部署目录（`$DSH_HOME/plugins/dsh-sound-lab`）、浏览器端模块 id 同步更新。
- **AI 生成角色音频**：配置弹窗音效库上方新增「🎙 AI生成角色音频」弹窗——
  - 详细教程可展开（含配图），指引获取阿里云百炼 API Key 与复刻音色ID；弹窗 4:3 横版，教程展开时最高 4:6、不超过页面高度；
  - 凭据三档状态提示：未配置（缺任一）/ 未通过（全配置但调用失败）/ 配置成功；已配置成功也可随时修改；
  - 生成时可自定义文件名（重名 409 拦截），生成音频可试听 / 删除，**一键加入音效库自动登记**（不复制文件，直接引用）；
  - 凭据防抖合并保存 + 生成请求直接携带 + 关闭弹窗强制落盘，杜绝「填了却提示未配置」；
  - 宿主端移植百炼 CosyVoice 合成协议（长文本分段 + WAV 帧拼接），token 消耗由用户自负。
- **音效库重构为 manifest 控制**：新增 `sounds/sounds.json` 控制文件驱动音效库，磁盘文件未登记时自动对账补录（放进目录即可用）；
  生成目录移到 `sounds/generated/`，manifest 支持 `generated/<file>` 路径直接引用，不再复制文件；
  音效分三类：`ai`（AI 生成，带标记）/ `local`（上传/手动放入）/ `bundled`（附带，不可删除/重命名）。
- **附带音效升级为 3 个**：新增「啊哇哇！」，与「hirari do～」「呢？」同为内置附带音效，不可删除/重命名（删除=软隐藏可随时恢复）。
- **音效重命名**：非附带音效可在列表中 ✎ 行内重命名（自动补扩展名、重名 409、联动触发条件引用）。
- **隐藏音效实时试听**：隐藏列表（附带音效）可直接试听且秒出——启动时预加载全部音效（含隐藏）进缓存，试听复用缓存不再二次下载。
- **UI 微调**：
  - 「自定义」按钮改名「上传」；上传成功自动关闭弹窗，恢复隐藏音效不关闭；隐藏列表实时同步删除/恢复；
  - 音效库超过 4 条时上下滚动（≤180px 内滚动），不超出配置页高度；
  - 音效名称不显示文件后缀（悬浮 title 保留完整名）；
  - 触发条件下拉「不播放」固定置顶；
  - AI 弹窗按状态动态展示输入框（未配置才显示），文本输入区自动占满剩余空间，整体无明显空位。
- **音频 URL 统一**：AI 生成音频统一走 `/sounds/generated/xxx`，各弹窗共享浏览器缓存，试听秒出。
- 工程化：宿主端 TTS 接口 + manifest 对账/重命名/登记冒烟测试扩充（43 项）；教程图片随包发布（lib/tutorial/）。

## 1.1.0 (2026-08-17)

- 触发条件音效下拉重构为「内置提示音 / 不播放 / 具体音效」三种选择：
  - 内置提示音 = Web Audio 琶音（零音频资源依赖），可独立选用；
  - 「不播放」可显式静音单个条件（原默认空值语义调整）；
  - 测试音效区可试听具体音效与内置提示音。
- 响度对齐：音效库音频播放统一乘 0.6 音量系数，内置提示音增益放大，两者听感接近。
- 默认配置：音量 75%；会话结束 →「hirari do～」、弹出选项/请求许可 →「呢？」、停止 → 内置提示音。
- 配置加载升级为逐字段 sanitize：类型校验 + 范围钳制 + 枚举过滤，脏数据回退默认值，
  同时作用于 localStorage 与宿主端 config.json。
- 注意类事件（弹出选项 / 请求许可）始终响铃：出现即播，不受会话运行/查看状态限制，
  且优先于完成类（会话结束/停止）的结束判定，避免连响。
- 工程化：新增 Node 冒烟测试（tools/test-host.mjs 覆盖宿主端 list/config/upload/delete 接口，
  tools/test-client.mjs 覆盖浏览器端 sanitize / 触发播放 / 默认配置），
  补充 `index.d.ts` 类型声明与 `npm test` 一键回归。

## 1.0.0 (2026-08-14)

- 初始版本：DSH Web GUI 语音控制插件，对话「会话结束 / 弹出选项 / 请求许可 / 停止」时播放指定音效。
- 可拖动悬浮球（靠边缩成半球）、可移动置顶配置弹窗；音效来自插件包 sounds/ 文件夹的本地音频。
- 支持鲸鱼娘 / 纯白 / 纯黑三种外观；配置自动保存（浏览器 + 宿主 config.json 双持久化）；启动即预加载。
- 附带默认音效「hirari do～」「呢？」；未选择/加载失败时 Web Audio 内置提示音兜底。
- 自定义音效：选择/拖拽上传音频进 sounds/ 目录，删除（附带音效软删可恢复）。
